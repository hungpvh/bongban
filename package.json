{
  "name": "bongban-vanilla",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "serve -p 3000 .",
    "build": "mkdir -p dist && cp index.html dist/ && cp -r js dist/ 2>/dev/null || true",
    "start": "serve -p 3000 dist"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
